Proompty:
- add
- get
- list
- remove
- set
Proompty add:
- --template-file
- --description
- --tags
- --template-content
- --set-var
- --set-var-value
Proompty get:
- $(find $PROOMPTY_DIR/proomplates/ -type f | sed 's|''$PROOMPTY_DIR/proomplates/||g')
- --template-file
- --prefix-mod
- --prefix
- --pm
- -p
- --suffix-mod
- --suffix
- --sm
- -s
- --append
- --clip
- --no-clip
- --output
- --no-output

Proompty get*--prefix-mod:
- $(find /mods/ -type f | sed 's|''/mods/||g')

Proompty get*--prefix:
- $(find /mods/ -type f | sed 's|''/mods/||g')

Proompty get*--pm:
- $(find /mods/ -type f | sed 's|''/mods/||g')
Proompty get*--suffix-mod:
- $(find /mods/ -type f | sed 's|''/mods/||g')

Proompty get*--suffix:
- $(find /mods/ -type f | sed 's|''/mods/||g')

Proompty get*--sm:
- $(find /mods/ -type f | sed 's|''/mods/||g')
Proompty list:
- --prompt-dir
Proompty remove:
Proompty set:
- $(find /proomplates/ -type f | sed 's|''/proomplates/||g')

