Proompty:
- add
- get
- list
- remove
- set
Proompty add:
- --template-file
- --description
- --tags
- --template-content
- --set-var
- --set-var-value
Proompty get:
- $(find $PROOMPTY_DIR/proomplates/ -type f | sed 's|'$PROOMPTY_DIR'/proomplates/||g')
- --template-file
- --prefix-mod
- --prefix
- --pm
- -p
- --suffix-mod
- --suffix
- --sm
- -s

Proompty get*--prefix-mod:
- $(find $PROOMPTY_DIR/mods/ -type f | sed 's|'$PROOMPTY_DIR'/mods/||g')

Proompty get*--prefix:
- $(find $PROOMPTY_DIR/mods/ -type f | sed 's|'$PROOMPTY_DIR'/mods/||g')

Proompty get*--pm:
- $(find $PROOMPTY_DIR/mods/ -type f | sed 's|'$PROOMPTY_DIR'/mods/||g')
Proompty get*--suffix-mod:
- $(find $PROOMPTY_DIR/mods/ -type f | sed 's|'$PROOMPTY_DIR'/mods/||g')

Proompty get*--suffix:
- $(find $PROOMPTY_DIR/mods/ -type f | sed 's|'$PROOMPTY_DIR'/mods/||g')

Proompty get*--sm:
- $(find $PROOMPTY_DIR/mods/ -type f | sed 's|'$PROOMPTY_DIR'/mods/||g')
Proompty list:
- --prompt-dir
Proompty remove:
Proompty set:
- $(find $PROOMPTY_DIR/proomplates/ -type f | sed 's|'$PROOMPTY_DIR'/proomplates/||g')

